# @package _group_
data_type: mnist
binarise_dataset: threshold
# load_data_from_pkl: 0

# Automatic download applicable if MNIST files could not be found
# Use top-level directory path for the repo as mnist_input
pre_processed_input_file: 0
mnist_input: /home/akajal/DiVAE

load_data_from_pkl: 0
frac_test_dataset: 0.5
frac_train_dataset: 1